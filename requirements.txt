# Core
torch>=2.1.0
torchvision>=0.16.0
ultralytics>=8.2.0        # YOLOv8 / newer

# Data handling
numpy
pandas
opencv-python
Pillow
pyyaml
scikit-learn

# Kaggle download
kaggle

# Metrics / evaluation
pycocotools
torchmetrics
scipy
statsmodels

# Explainability
grad-cam

# Robustness / augmentation
# NOTE: albumentations is installed as a transitive dependency of ultralytics,
# but notebook 03 explicitly UNINSTALLS it before YOLO training so that its
# default low-probability transforms (Blur/MedianBlur/ToGray/CLAHE) are
# skipped, keeping augmentation equivalent to the Faster R-CNN pipeline.
albumentations

# Visualization
matplotlib
seaborn

# Notebooks
jupyter
tqdm
